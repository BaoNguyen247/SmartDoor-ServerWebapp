// Placeholder for Access History functionality
// Currently, the history table is static, but this can be extended to fetch and display dynamic data
function initializeHistory() {
    console.log('Access History section initialized');
}